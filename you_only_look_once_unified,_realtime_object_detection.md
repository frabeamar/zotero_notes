YOLO sees the entire image so it encodes contextual information, but a RPN (region proposal network is also a cnn with access on the entire image [this had a name]

why does each grid cell produce two boxes? you have one for each class?

DPM: deformable parts model

Training for a week. Modern architecture are about the same